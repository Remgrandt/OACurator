# Copyright (c) 2026 Remgrandt Works. All rights reserved.

name: macOS Release

on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Release tag. Leave blank to use beta-macos-${run_number}."
        required: false
        type: string
      release_name:
        description: "Release title. Leave blank to use OA Curator ${tag_name}."
        required: false
        type: string
      draft:
        description: "Create the GitHub Release as a draft when the release does not already exist."
        required: true
        type: boolean
        default: true
      prerelease:
        description: "Mark the GitHub Release as a prerelease when the release does not already exist."
        required: true
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: macos-release
  cancel-in-progress: false

env:
  MACOSX_DEPLOYMENT_TARGET: "10.15"
  MACOS_ARM64_DEPLOYMENT_TARGET: "11.0"

jobs:
  source-check:
    name: Check macOS release source
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-docs.txt

      - name: Install frontend dependencies
        run: npm ci

      - name: Install documentation dependencies
        run: python -m pip install -r requirements-docs.txt

      - name: Check release source
        run: npm run check:release

  libvips-runtime:
    name: Bundle macOS libvips runtime (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    needs: source-check

    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: arm64
            runner: macos-26
            expected_file_arch: arm64
          - arch: x64
            runner: macos-26-intel
            expected_file_arch: x86_64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Bundle libvips runtime
        run: bash scripts/install-libvips-macos.sh

      - name: Verify staged libvips runtime
        shell: bash
        run: |
          set -euo pipefail
          libvips_dir="src-tauri/resources/libvips"

          test -x "$libvips_dir/vips"
          test -x "$libvips_dir/vipsheader"

          file "$libvips_dir/vips"
          file "$libvips_dir/vips" | grep -q "${{ matrix.expected_file_arch }}"
          "$libvips_dir/vips" --version

          if find "$libvips_dir" -type f -perm -111 -print0 |
            xargs -0 otool -L |
            grep -E '/(opt/homebrew|usr/local)/(Cellar|opt)/|/opt/local/' ; then
            echo "::error::Bundled libvips still references package-manager library paths."
            exit 1
          fi

          smoke_source="$RUNNER_TEMP/oac-libvips-smoke.tif"
          smoke_output="$RUNNER_TEMP/oac-libvips-smoke.png"
          "$libvips_dir/vips" black "$smoke_source" 4096 4096 --bands 3
          "$libvips_dir/vips" thumbnail "$smoke_source" "$smoke_output" 800 --height 800 --size down
          test "$("$libvips_dir/vipsheader" -f height "$smoke_output")" = "800"

      - name: Archive libvips runtime
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p runtime-artifacts
          tar -czf "runtime-artifacts/libvips-${{ matrix.arch }}.tar.gz" -C src-tauri/resources libvips

      - name: Upload libvips runtime
        uses: actions/upload-artifact@v4
        with:
          name: oa-curator-macos-libvips-${{ matrix.arch }}
          path: runtime-artifacts/*
          if-no-files-found: error

  macos-universal:
    name: Build signed and notarized universal macOS app
    runs-on: macos-26
    needs: libvips-runtime

    env:
      APPLE_SIGNING_IDENTITY: "Developer ID Application: Remgrandt Games LLC (3GHU5785J6)"
      APPLE_TEAM_ID: "3GHU5785J6"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Confirm official release repository
        shell: bash
        run: |
          set -euo pipefail
          if [ "$GITHUB_REPOSITORY" != "Remgrandt/OACurator" ]; then
            echo "::error::This workflow publishes official OA Curator releases only from Remgrandt/OACurator."
            exit 1
          fi

      - name: Validate signing and notarization secrets
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE_P12_BASE64 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER_ID }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_KEY_P8_BASE64: ${{ secrets.APPLE_API_KEY_P8_BASE64 }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        shell: bash
        run: |
          set -euo pipefail
          missing=0
          for name in \
            APPLE_CERTIFICATE \
            APPLE_CERTIFICATE_PASSWORD \
            APPLE_API_ISSUER \
            APPLE_API_KEY \
            APPLE_API_KEY_P8_BASE64 \
            TAURI_SIGNING_PRIVATE_KEY \
            TAURI_SIGNING_PRIVATE_KEY_PASSWORD
          do
            if [ -z "${!name}" ]; then
              echo "::error::Missing required secret for $name."
              missing=1
            fi
          done
          exit "$missing"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-docs.txt

      - name: Install frontend dependencies
        run: npm ci

      - name: Install documentation dependencies
        run: python -m pip install -r requirements-docs.txt

      - name: Download architecture libvips runtimes
        uses: actions/download-artifact@v4
        with:
          pattern: oa-curator-macos-libvips-*
          path: ${{ runner.temp }}/libvips-inputs

      - name: Make universal libvips runtime
        shell: bash
        run: |
          set -euo pipefail
          arm64_extract="$RUNNER_TEMP/libvips-arm64"
          x64_extract="$RUNNER_TEMP/libvips-x64"
          mkdir -p "$arm64_extract" "$x64_extract"

          tar -xzf "$RUNNER_TEMP/libvips-inputs/oa-curator-macos-libvips-arm64/libvips-arm64.tar.gz" -C "$arm64_extract"
          tar -xzf "$RUNNER_TEMP/libvips-inputs/oa-curator-macos-libvips-x64/libvips-x64.tar.gz" -C "$x64_extract"

          bash scripts/make-universal-libvips-macos.sh \
            "$arm64_extract/libvips" \
            "$x64_extract/libvips" \
            "src-tauri/resources/libvips"

          bash scripts/verify-macos-universal-runtime.sh \
            "src-tauri/resources/libvips" \
            "$MACOSX_DEPLOYMENT_TARGET" \
            "$MACOS_ARM64_DEPLOYMENT_TARGET"

          smoke_source="$RUNNER_TEMP/oac-universal-libvips-smoke.tif"
          smoke_output="$RUNNER_TEMP/oac-universal-libvips-smoke.png"
          "src-tauri/resources/libvips/vips" black "$smoke_source" 4096 4096 --bands 3
          "src-tauri/resources/libvips/vips" thumbnail "$smoke_source" "$smoke_output" 800 --height 800 --size down
          test "$("src-tauri/resources/libvips/vipsheader" -f height "$smoke_output")" = "800"

      - name: Import Apple Developer ID certificate
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE_P12_BASE64 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
        shell: bash
        run: |
          set -euo pipefail
          certificate_path="$RUNNER_TEMP/developer-id-application.p12"
          keychain_path="$RUNNER_TEMP/oacurator-signing.keychain-db"
          keychain_password="$(uuidgen)"

          CERTIFICATE_PATH="$certificate_path" python - <<'PY'
          import base64
          import os
          from pathlib import Path

          Path(os.environ["CERTIFICATE_PATH"]).write_bytes(
              base64.b64decode(os.environ["APPLE_CERTIFICATE"])
          )
          PY
          security create-keychain -p "$keychain_password" "$keychain_path"
          security set-keychain-settings -lut 21600 "$keychain_path"
          security unlock-keychain -p "$keychain_password" "$keychain_path"
          security import "$certificate_path" -P "$APPLE_CERTIFICATE_PASSWORD" -A -t cert -f pkcs12 -k "$keychain_path" -T /usr/bin/codesign
          security list-keychains -d user -s "$keychain_path"
          security default-keychain -s "$keychain_path"
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$keychain_password" "$keychain_path"
          security find-identity -v -p codesigning "$keychain_path"

          echo "KEYCHAIN_PATH=$keychain_path" >> "$GITHUB_ENV"

      - name: Sign staged universal libvips runtime
        shell: bash
        run: |
          set -euo pipefail
          libvips_dir="src-tauri/resources/libvips"

          while IFS= read -r -d '' mach_o; do
            if file "$mach_o" | grep -Eq 'Mach-O'; then
              codesign --force --timestamp --options runtime --sign "$APPLE_SIGNING_IDENTITY" "$mach_o"
              codesign --verify --verbose=2 "$mach_o"
            fi
          done < <(find "$libvips_dir" -type f -print0)

      - name: Prepare App Store Connect API key
        env:
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_KEY_P8_BASE64: ${{ secrets.APPLE_API_KEY_P8_BASE64 }}
        shell: bash
        run: |
          set -euo pipefail
          private_key_dir="$RUNNER_TEMP/private_keys"
          private_key_path="$private_key_dir/AuthKey_${APPLE_API_KEY}.p8"
          mkdir -p "$private_key_dir"
          APPLE_API_KEY_PATH="$private_key_path" python - <<'PY'
          import base64
          import os
          from pathlib import Path

          Path(os.environ["APPLE_API_KEY_PATH"]).write_bytes(
              base64.b64decode(os.environ["APPLE_API_KEY_P8_BASE64"])
          )
          PY
          chmod 600 "$private_key_path"
          echo "APPLE_API_KEY_PATH=$private_key_path" >> "$GITHUB_ENV"

      - name: Build signed and notarized universal macOS package
        env:
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER_ID }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY_ID }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        run: npm run release:macos

      - name: Verify universal macOS signatures and notarization
        shell: bash
        run: |
          set -euo pipefail
          bundle_dir="src-tauri/target/universal-apple-darwin/release/bundle"
          dmg_file="$(find "$bundle_dir" -type f -name '*_universal.dmg' | head -n 1)"
          mount_dir="$RUNNER_TEMP/oacurator-dmg"

          if [ -z "$dmg_file" ]; then
            echo "::error::No universal .dmg file was produced."
            exit 1
          fi

          codesign --verify --verbose=2 "$dmg_file"

          mkdir -p "$mount_dir"
          hdiutil attach "$dmg_file" -readonly -nobrowse -mountpoint "$mount_dir"
          trap 'hdiutil detach "$mount_dir" -quiet || true' EXIT

          app_bundle="$(find "$mount_dir" -type d -name '*.app' | head -n 1)"
          if [ -z "$app_bundle" ]; then
            echo "::error::No .app bundle was found inside the DMG."
            exit 1
          fi

          codesign --verify --deep --strict --verbose=2 "$app_bundle"
          spctl --assess --type execute --verbose=4 "$app_bundle"
          xcrun stapler validate "$app_bundle"

          app_binary="$app_bundle/Contents/MacOS/oa-curator"
          if [ ! -x "$app_binary" ]; then
            echo "::error::The app bundle does not contain the expected executable."
            exit 1
          fi
          lipo -verify_arch arm64 x86_64 "$app_binary"
          bash scripts/verify-macos-universal-runtime.sh "$app_bundle" "$MACOSX_DEPLOYMENT_TARGET" "$MACOS_ARM64_DEPLOYMENT_TARGET"

          libvips_dir="$app_bundle/Contents/Resources/resources/libvips"
          if [ ! -x "$libvips_dir/vips" ] || [ ! -x "$libvips_dir/vipsheader" ]; then
            echo "::error::The app bundle does not contain executable libvips tools."
            exit 1
          fi
          bash scripts/verify-macos-universal-runtime.sh "$libvips_dir" "$MACOSX_DEPLOYMENT_TARGET" "$MACOS_ARM64_DEPLOYMENT_TARGET"
          "$libvips_dir/vips" --version

          smoke_source="$RUNNER_TEMP/oac-bundled-libvips-smoke.tif"
          smoke_output="$RUNNER_TEMP/oac-bundled-libvips-smoke.png"
          "$libvips_dir/vips" black "$smoke_source" 4096 4096 --bands 3
          "$libvips_dir/vips" thumbnail "$smoke_source" "$smoke_output" 800 --height 800 --size down
          test "$("$libvips_dir/vipsheader" -f height "$smoke_output")" = "800"

      - name: Collect macOS release assets
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p release-assets
          bundle_dir="src-tauri/target/universal-apple-darwin/release/bundle"
          find "$bundle_dir" -type f \( -name '*.dmg' -o -name '*.app.tar.gz' -o -name '*.app.tar.gz.sig' \) -print0 |
            while IFS= read -r -d '' file; do
              name="$(basename "$file" | tr ' ' '.')"
              cp "$file" "release-assets/$name"
            done

          dmg_count="$(find release-assets -maxdepth 1 -type f -name '*_universal.dmg' | wc -l | tr -d ' ')"
          updater_count="$(find release-assets -maxdepth 1 -type f -name '*.app.tar.gz' | wc -l | tr -d ' ')"
          signature_count="$(find release-assets -maxdepth 1 -type f -name '*.app.tar.gz.sig' | wc -l | tr -d ' ')"

          if [ "$dmg_count" -ne 1 ]; then
            echo "::error::Expected exactly one universal macOS DMG, found $dmg_count."
            exit 1
          fi
          if [ "$updater_count" -ne 1 ] || [ "$signature_count" -ne 1 ]; then
            echo "::error::Expected exactly one macOS updater archive and signature."
            exit 1
          fi

      - name: Upload macOS build artifact
        uses: actions/upload-artifact@v4
        with:
          name: oa-curator-macos-universal-release-assets
          path: release-assets/*
          if-no-files-found: error

      - name: Delete temporary signing keychain
        if: always()
        shell: bash
        run: |
          if [ -n "${KEYCHAIN_PATH:-}" ] && [ -f "$KEYCHAIN_PATH" ]; then
            security delete-keychain "$KEYCHAIN_PATH"
          fi

  publish:
    name: Publish macOS release artifacts
    runs-on: ubuntu-latest
    needs: macos-universal

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Confirm official release repository
        shell: bash
        run: |
          set -euo pipefail
          if [ "$GITHUB_REPOSITORY" != "Remgrandt/OACurator" ]; then
            echo "::error::This workflow publishes official OA Curator releases only from Remgrandt/OACurator."
            exit 1
          fi

      - name: Resolve release metadata
        id: release_meta
        shell: bash
        run: |
          set -euo pipefail
          tag="${{ inputs.tag_name }}"
          tag="${tag#"${tag%%[![:space:]]*}"}"
          tag="${tag%"${tag##*[![:space:]]}"}"
          if [ -z "$tag" ]; then
            tag="beta-macos-${{ github.run_number }}"
          fi

          title="${{ inputs.release_name }}"
          title="${title#"${title%%[![:space:]]*}"}"
          title="${title%"${title##*[![:space:]]}"}"
          if [ -z "$title" ]; then
            title="OA Curator $tag"
          fi

          {
            echo "tag_name=$tag"
            echo "release_name=$title"
          } >> "$GITHUB_OUTPUT"

      - name: Download macOS build artifacts
        uses: actions/download-artifact@v4
        with:
          name: oa-curator-macos-universal-release-assets
          path: release-assets

      - name: Generate updater manifest
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          release_tag="${{ steps.release_meta.outputs.tag_name }}"
          existing_latest_dir="$RUNNER_TEMP/oacurator-existing-latest"
          existing_latest_path="$existing_latest_dir/latest.json"
          mkdir -p "$existing_latest_dir"

          if gh release view "$release_tag" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release download "$release_tag" \
              --repo "$GITHUB_REPOSITORY" \
              --pattern latest.json \
              --dir "$existing_latest_dir" \
              --clobber || true
          fi

          RELEASE_TAG="$release_tag" EXISTING_LATEST_PATH="$existing_latest_path" python - <<'PY'
          import datetime as dt
          import json
          import os
          from pathlib import Path

          release_assets = Path("release-assets")
          config = json.loads(Path("src-tauri/tauri.conf.json").read_text(encoding="utf-8")) if Path("src-tauri/tauri.conf.json").exists() else {"version": "0.1.2"}
          existing_latest_path = Path(os.environ["EXISTING_LATEST_PATH"])
          if existing_latest_path.exists():
              latest = json.loads(existing_latest_path.read_text(encoding="utf-8"))
          else:
              latest = {}

          default_notes = "\n".join(
              [
                  "OA Curator public beta build.",
                  "",
                  f"Source commit: {os.environ['GITHUB_SHA']}",
                  "License: source-available freeware.",
              ]
          )
          latest["version"] = config["version"]
          if not str(latest.get("notes", "")).strip():
              latest["notes"] = default_notes
          if not str(latest.get("pub_date", "")).strip():
              latest["pub_date"] = (
                  dt.datetime.now(dt.timezone.utc)
                  .replace(microsecond=0)
                  .isoformat()
                  .replace("+00:00", "Z")
              )

          platforms = latest.get("platforms")
          if not isinstance(platforms, dict):
              platforms = {}

          updater_artifacts = sorted(release_assets.glob("*.app.tar.gz"))
          if len(updater_artifacts) != 1:
              raise SystemExit(
                  f"Expected one universal macOS updater archive, found {len(updater_artifacts)}."
              )

          universal_updater_artifact = updater_artifacts[0]
          signature_path = Path(f"{universal_updater_artifact}.sig")
          if not signature_path.exists():
              raise SystemExit(f"No updater signature found for {universal_updater_artifact.name}.")

          asset_url = (
              f"https://github.com/{os.environ['GITHUB_REPOSITORY']}/"
              f"releases/download/{os.environ['RELEASE_TAG']}/{universal_updater_artifact.name}"
          )
          macos_update = {
              "signature": signature_path.read_text(encoding="utf-8").strip(),
              "url": asset_url,
          }
          platforms["darwin-aarch64"] = macos_update
          platforms["darwin-x86_64"] = macos_update

          latest["platforms"] = platforms

          (release_assets / "latest.json").write_text(
              json.dumps(latest, indent=2) + "\n",
              encoding="utf-8",
          )
          PY

      - name: Generate checksums
        shell: bash
        run: |
          set -euo pipefail
          shasum -a 256 release-assets/* > release-assets/SHA256SUMS-macos.txt

      - name: Create or update GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          tag="${{ steps.release_meta.outputs.tag_name }}"
          title="${{ steps.release_meta.outputs.release_name }}"
          notes="$(cat <<'NOTES'
          OA Curator public beta build.

          Source commit: $GITHUB_SHA
          License: source-available freeware.
          NOTES
          )"
          notes="${notes//\$GITHUB_SHA/$GITHUB_SHA}"

          if gh release view "$tag" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release upload "$tag" release-assets/* --repo "$GITHUB_REPOSITORY" --clobber
          else
            release_args=(
              release create "$tag"
              release-assets/*
              --repo "$GITHUB_REPOSITORY"
              --target "$GITHUB_SHA"
              --title "$title"
              --notes "$notes"
            )

            if [ "${{ inputs.draft }}" = "true" ]; then
              release_args+=(--draft)
            fi
            if [ "${{ inputs.prerelease }}" = "true" ]; then
              release_args+=(--prerelease)
            fi

            gh "${release_args[@]}"
          fi
